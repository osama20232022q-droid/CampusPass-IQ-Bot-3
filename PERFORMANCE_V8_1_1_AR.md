# CampusPass IQ V8.1.1 — Performance Rocket

إصدار تحسين أداء جراحي فوق V8.1 دون حذف ميزات أو تغيير منطق الطلبات أو المحفظة أو التسويات أو الصلاحيات.

## التحسينات
- Cache قصير (2 ثانية) لإعدادات القوائم التي كانت تتطلب عدة استعلامات متتالية إلى PostgreSQL عند كل تنقل.
- إبطال Cache فور أي تعديل من المالك على النص/المكان/النوع/الحالة حتى تظهر التغييرات مباشرة.
- Cache قصير (3 ثوانٍ) لأعلام الميزات مع إبطال فوري عند Toggle.
- Cache قصير لدور المستخدم في بناء القوائم فقط لتقليل استعلامات ProviderStaff المتكررة.
- حل ضغط أزرار Reply Keyboard من Snapshot محلي بدلاً من استعلام قاعدة بيانات لكل ضغطة.
- حل أزرار Inline حسب المفتاح من Snapshot نفسه.
- إزالة استعلام إضافي عند بناء أزرار Back داخل القوائم الفرعية.
- Warm-up للقائمة وميزة الألوان أثناء Startup حتى لا يدفع أول مستخدم تكلفة تحميلها.
- تفعيل pool_use_lifo لاتصالات PostgreSQL مع الإبقاء على pool_pre_ping وحدود pool الحالية لضمان الاستقرار.

## Zero Regression
لم يتم حذف Handler أو Service أو Migration أو Feature. لم تتغير قواعد الدفع أو المحفظة أو الحظر أو التسوية.

## التحقق
- Python compileall: ناجح.
- فحص بنيوي للحزمة: ناجح.
- تعذر تشغيل pytest الكامل في بيئة الإنشاء الحالية لأن اعتماد aiogram غير مثبت محلياً؛ Railway يثبته من requirements.txt أثناء البناء.
