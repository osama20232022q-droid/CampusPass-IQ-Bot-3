# تقرير إصدار CampusPass IQ V8.1

الإصدار: `8.1.0-ux-wallet-settlement`

## تغييرات إنتاجية
1. **Wallet Ledger**: قبول الزيادة وحفظها في محفظة المستخدم، واستخدام الرصيد فقط لتغطية النقص بالكامل.
2. **Order Coupons**: أكواد خصم عامة أو مرتبطة بمنصة؛ الخصم يطبق على سعر الخدمة ولا يمس رسوم CampusPass افتراضياً.
3. **Owner Collections**: المالك يختار المنصة والمبلغ من الرسوم المستحقة فعلياً، والمنصة ترسل صورة وصل MasterCard. المالك يملك أزرار تأكيد/رفض/حظر.
4. **Provider Suspension**: الحظر Soft Suspension وليس حذفاً مادياً؛ العروض تختفي عن الطلاب، والطلبات المفتوحة تتوقف ويُخطر أصحابها. البيانات التاريخية تبقى للتدقيق.
5. **Provider Reactivation**: إذا كان الحظر سببه طلب التحصيل نفسه ثم تم تأكيد الدفع، تعاد المنصة وعروضها التي أخفاها ذلك الحظر فقط؛ الطلبات الملغاة لا تعاد تلقائياً.
6. **User Administrative Record**: بيانات المستخدم، حالة الحظر، عدد الطلبات، الاشتراكات، ورصيد المحفظة متاحة للمالك فقط.
7. **Reports**: شعار المنصة يُخزن كـ data URI داخل قاعدة البيانات عند رفعه كصورة، لذلك لا يختفي بعد Restart؛ التقرير يرفض الإنشاء دون شعار.
8. **Menu UX**: زر «حسابي»، تحريك الأزرار بالأسهم، وإزالة رسالة إلغاء الخطوة أثناء التنقل الطبيعي.
9. **Gemini**: فصل مفتاح الاتصال عن Feature Flag وإظهار سبب عدم توفر المساعد بصورة أدق.

## اختبارات تم تنفيذها محلياً
- `python -m compileall -q app` — ناجح.
- `verify_runtime_files.py` — ناجح.
- `verify_project.py` — ناجح.
- Phase 3 / 4 / 5 / 6 / 7B verifiers — ناجحة.
- اختبارات V8.1 الجديدة + core + V8.0B: **11 passed, 1 skipped**.

## ما لا يمكن تأكيده محلياً
الاتصال الحي الحقيقي بـ Telegram وNeon وGemini يحتاج Deploy على Railway. لذلك بعد الرفع يجب فحص Deploy Logs وتجربة طلب تجريبي واحد من البداية للنهاية قبل استعمال بيانات حقيقية.
